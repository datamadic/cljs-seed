# add clj check
# add full path dir to scripts path
# add init default name check? use dir name?

alias build='clj -i scripts/build.clj'
alias release='clj -i scripts/release.clj'
alias repl='clj -i scripts/repl.clj'
alias pwatch='clj -i scripts/watch.clj'
